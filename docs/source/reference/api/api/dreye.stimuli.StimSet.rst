dreye.stimuli.StimSet
=====================

.. currentmodule:: dreye.stimuli

.. autoclass:: StimSet

   


..
   HACK -- the point here is that we don't want this to appear in the output, but the autosummary should still generate the pages.
   .. autosummary::
      :toctree:
      
      StimSet.create
      StimSet.from_dict
      StimSet.load
      StimSet.load_settings
      StimSet.plot
      StimSet.save
      StimSet.save_settings
      StimSet.time2frame
      StimSet.to_dict
      StimSet.transform




   


..
   HACK -- the point here is that we don't want this to appear in the output, but the autosummary should still generate the pages.
   .. autosummary::
      :toctree:
      
      StimSet.ch_names
      StimSet.channel_axis
      StimSet.channel_len
      StimSet.duration
      StimSet.events
      StimSet.fitted_signal
      StimSet.metadata
      StimSet.name
      StimSet.other_shape
      StimSet.settings
      StimSet.signal
      StimSet.stimulus
      StimSet.time_axis
      StimSet.time_len
      StimSet.timestamps


